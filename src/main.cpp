#include <iostream>
#include <windows.h>

int main() {
    std::cout << "WinPulse is initializing..." << std::endl;
    std::cout << "Press Enter to exit." << std::endl;
    std::cin.get();
    return 0;
}